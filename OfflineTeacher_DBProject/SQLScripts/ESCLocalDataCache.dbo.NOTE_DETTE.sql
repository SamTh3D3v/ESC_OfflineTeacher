/****
This SQL script was generated by the Configure Data Synchronization
dialog box. This script contains statements that create the
change-tracking columns, deleted-items table, and triggers on the
server database. These database objects are required by Synchronization
Services to successfully synchronize data between client and server
databases. For more information, see the
‘How to: Configure a Database Server for Synchronization’ topic in Help.
****/


IF @@TRANCOUNT > 0
set ANSI_NULLS ON 
set QUOTED_IDENTIFIER ON 

GO
BEGIN TRANSACTION;


IF @@TRANCOUNT > 0
ALTER TABLE [dbo].[NOTE_DETTE] 
ADD [LastEditDate] DateTime NULL CONSTRAINT [DF_NOTE_DETTE_LastEditDate] DEFAULT (GETUTCDATE()) WITH VALUES
GO
IF @@ERROR <> 0 
     ROLLBACK TRANSACTION;


IF @@TRANCOUNT > 0
ALTER TABLE [dbo].[NOTE_DETTE] 
ADD [CreationDate] DateTime NULL CONSTRAINT [DF_NOTE_DETTE_CreationDate] DEFAULT (GETUTCDATE()) WITH VALUES
GO
IF @@ERROR <> 0 
     ROLLBACK TRANSACTION;


IF @@TRANCOUNT > 0
IF EXISTS (SELECT * FROM sys.objects WHERE object_id = OBJECT_ID(N'[dbo].[NOTE_DETTE_UpdateTrigger]') AND type = 'TR') 
   DROP TRIGGER [dbo].[NOTE_DETTE_UpdateTrigger] 

GO
CREATE TRIGGER [dbo].[NOTE_DETTE_UpdateTrigger] 
    ON [dbo].[NOTE_DETTE] 
    AFTER UPDATE 
AS 
BEGIN 
    SET NOCOUNT ON 
    UPDATE [dbo].[NOTE_DETTE] 
    SET [LastEditDate] = GETUTCDATE() 
    FROM inserted 
    WHERE inserted.[ID_ETUDIANT] = [NOTE_DETTE].[ID_ETUDIANT] 
    AND inserted.[ANNEE_UNIVERSITAIRE] = [NOTE_DETTE].[ANNEE_UNIVERSITAIRE] 
    AND inserted.[ID_MATIERE] = [NOTE_DETTE].[ID_MATIERE] 
    AND inserted.[ANNEE_PASSAGE_DETTE] = [NOTE_DETTE].[ANNEE_PASSAGE_DETTE] 
END;
GO
IF @@ERROR <> 0 
     ROLLBACK TRANSACTION;


IF @@TRANCOUNT > 0
IF EXISTS (SELECT * FROM sys.objects WHERE object_id = OBJECT_ID(N'[dbo].[NOTE_DETTE_InsertTrigger]') AND type = 'TR') 
   DROP TRIGGER [dbo].[NOTE_DETTE_InsertTrigger] 

GO
CREATE TRIGGER [dbo].[NOTE_DETTE_InsertTrigger] 
    ON [dbo].[NOTE_DETTE] 
    AFTER INSERT 
AS 
BEGIN 
    SET NOCOUNT ON 
    UPDATE [dbo].[NOTE_DETTE] 
    SET [CreationDate] = GETUTCDATE() 
    FROM inserted 
    WHERE inserted.[ID_ETUDIANT] = [NOTE_DETTE].[ID_ETUDIANT] 
    AND inserted.[ANNEE_UNIVERSITAIRE] = [NOTE_DETTE].[ANNEE_UNIVERSITAIRE] 
    AND inserted.[ID_MATIERE] = [NOTE_DETTE].[ID_MATIERE] 
    AND inserted.[ANNEE_PASSAGE_DETTE] = [NOTE_DETTE].[ANNEE_PASSAGE_DETTE] 
END;
GO
IF @@ERROR <> 0 
     ROLLBACK TRANSACTION;
COMMIT TRANSACTION;
